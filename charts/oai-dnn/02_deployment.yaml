apiVersion: apps/v1
kind: Deployment
metadata:
  name: oai-dnn10
  labels:
    app: oai-dnn10
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oai-dnn10
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: oai-dnn10
      annotations:
        k8s.v1.cni.cncf.io/networks: dnn-net10
    spec:
      nodeSelector:
        type: run
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        image: tolgaomeratalay/oai-dnn:4kaggrdu
      - name: dnn
        image: tolgaomeratalay/oai-dnn:4kaggrdu
        #command: ['bash', '-c', "iptables -t nat -A POSTROUTING -o net1 -j MASQUERADE; ip route add 12.1.0.0/16 via 192.168.15.10 dev net1;"]
        #command: ["/bin/bash"]
        #args: ["-c", "iptables -t nat -A POSTROUTING -o net1 -j MASQUERADE;ip route add 12.1.0.0/16 via 192.168.15.10 dev net1;"]
        #  - iptables -t nat -A POSTROUTING -o net1 -j MASQUERADE;
        #  - ip route add 12.1.0.0/16 via 192.168.15.10 dev net1;
        command:
          - sleep
          - infinity
        imagePullPolicy: IfNotPresent
        securityContext:
          privileged: true
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
